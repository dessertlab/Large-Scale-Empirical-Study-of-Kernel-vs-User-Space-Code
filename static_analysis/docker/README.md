# Analyzer Docker Images

This directory contains the Dockerfiles used by the `static_analysis` module.

Build all analyzer images from the repository root with:

```bash
bash init_sats.sh
```

The image names are configured in `config.toml`.
